{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"API,WEB\" -c \"cyan,magenta\" \"npm run dev --prefix backend\" \"npm start --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm start --prefix frontend",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "seed": "npm run seed --prefix backend",
    "install:all": "npm i --prefix frontend && npm i --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
